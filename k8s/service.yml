apiVersion: v1
kind: Service
metadata:
  name: wordpress
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "wordpress"
    port: 80
    targetPort: 80
  - name: ssh
    port: 22
    targetPort: 22122
  selector:
    app: wordpress
